'use client';

// The entire component has been simplified to isolate a persistent client-side error.

export default function AdminPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-4 bg-gray-100 dark:bg-gray-800">
      <h1 className="text-3xl font-bold mb-8 text-center">Admin Console</h1>
      <p>This page has been simplified for debugging purposes.</p>
      <p>If you can see this, the basic rendering is working.</p>
    </main>
  );
}
